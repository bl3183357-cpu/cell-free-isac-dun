[workspace]
name = "cell-free-isac-dun"
version = "0.1.0"
description = "Unsupervised Deep Unfolding for Cell-Free ISAC Beamforming"
authors = ["Your Name <your.email@example.com>"]
channels = ["pytorch", "nvidia", "conda-forge"]
# 新增了 osx-arm64，完美支持 M1/M2/M3/M4 Mac
platforms = ["linux-64", "osx-arm64"]

[tasks]
start = "python main.py"
train-heu = "python main.py --algo gnn_heuristic --alpha 0.5"
train-mrt = "python main.py --algo gnn_mrt --alpha 0.5"
train-zf = "python main.py --algo gnn_zf --alpha 0.5"
eval-zf = "python main.py --algo zf_only"

[dependencies]
python = "3.11.*" 
numpy = "<2.0.0"
scipy = "*"
matplotlib = "*"

# 传统算法 
cvxpy = "*"

# 全局环境只安装基础的 PyTorch（在 Mac 上会自动支持 MPS，在 Linux 上是 CPU 基础包）
pytorch = "*"
tqdm = ">=4.67.3,<5"
scikit-learn = ">=1.8.0,<2"
jupyter = ">=1.1.1,<2"
quarto = ">=1.4.557,<2"
nbformat = ">=5.10.4,<6"
nbclient = ">=0.10.4,<0.11"
ipykernel = ">=7.2.0,<8"
seaborn = ">=0.13.2,<0.14"
typst = ">=0.10.0,<0.11"
wandb = ">=0.25.1,<0.26"
pytorch-lightning = ">=2.6.1,<3"

# 【关键修改】：将 CUDA 设为仅 Linux 平台专属依赖
[target.linux-64.dependencies]
pytorch-cuda = "12.1.*"
